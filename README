README!
-------

Generate binary:
    ./configure --prefix=<INSTALL DIR> && make

Install:
    make install
    (if --prefix wasn't set in configure, install dir will be /usr/bin)

Usage:
    ./dem --help
